// Dp.Util.Math
(function (my) {
    var cents = 0;

    // Public properties and methods
    my.Math = {
        /**
         * Multiply 2 numbers
         */
        Multiply: function (a, b) {
            return a * b;
        },
        Divided: function (a, b) {
            return a / b;
        }
    };

    return my;
}(Dp.Util || {}));

